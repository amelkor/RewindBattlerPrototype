namespace Game
{
    /// <summary>
    /// Indicates where an animation callback will be fired on the animation clip timeline.
    /// </summary>
    public enum AnimationClipEventFirePosition
    {
        Custom = 0,
        AtStart,
        AtEnd,
        AtMiddle
    }
}